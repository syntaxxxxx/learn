package com.syntax.learn.screen.login

import android.content.Context
import android.content.Intent
import com.syntax.learn.common.Navigator

object LoginNavigator {
    class ForgotPassword : Navigator() {
        override fun navigate(context: Context) {
//            context.startActivity(Intent(context, WbForgetPassActivity::class.java))
        }
    }
}